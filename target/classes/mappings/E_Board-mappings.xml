<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="E_BoardDAO">
	
	<insert id="insertBoard">
	<!--  insert into E_Board(NUM, TITLE, CONTENT, ZIPCODE, ADDRESS, TRAF, PRICE, STARTDATE, ENDDATE,
	 			TEL, URI, WRITER, WRITEDATE, UPDATER, UPDATEDATE) 
				values((select nvl(max(seq),0)+1 from E_Board), #{title}, #{content}, #{zipcode},
				#{address}, #{traf}, #{price}, #{startdate}, #{enddate}, #{tel}, #{uri},
				#{writer}, sysdate #{updater}, sysydate)		-->
	insert into test123 values(3, 'abc');			
	</insert>
	<!--  
	<update id="updateBoard">
	update board set title=#{title}, content=#{content} where seq=#{seq}
	</update>
	
	<delete id="deleteBoard">
	delete from board where seq=#{seq}
	</delete>
	
	<select id="getBoard" resultType="board">
    select * from board where seq = #{seq}
	</select>
	
	<select id="getBoardList" resultType="board">
	select * from board
		<if test="searchKeyword != null">
		where title like '%'||#{searchKeyword}||'%' 
		</if>
	order by seq desc
	</select>
	-->
</mapper>
